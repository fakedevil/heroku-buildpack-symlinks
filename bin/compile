#!/bin/bash

set -e

BUILD_DIR=$1
CACHE_DIR=$2

indent() {
  sed -u 's/^/       /'
}

echo "-----> Symlink opencv test3"

ln -sf /usr/lib/jni/libopencv_java320.so $BUILD_DIR/.apt/usr/lib/libopencv_java.so
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/usr/lib/jni/:$BUILD_DIR/.apt/usr/lib/
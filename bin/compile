#!/bin/bash

set -e

BUILD_DIR=$1
CACHE_DIR=$2

indent() {
  sed -u 's/^/       /'
}

echo "-----> Symlink vendor binaries"

ln -sf $BUILD_DIR/vendor/phantomjs/bin/phantomjs $BUILD_DIR/bin/phantomjs

echo $BUILD_DIR





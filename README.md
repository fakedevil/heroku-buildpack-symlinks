# Heroku-buildpack-symlinks

This project is intended solely to create symbolic links in the file system.  
In particular, to binary files in the 'vendor' directory to reduce the length of the PATH variable.
